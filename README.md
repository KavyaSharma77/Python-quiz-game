# Python-quiz-game
This is a simple command-line quiz application built using Python and Object-Oriented Programming (OOP). The program loads a collection of quiz questions, presents them one by one to the user, checks each answer, and displays the final score at the end.
